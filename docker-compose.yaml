version: '3.4'
volumes:
  relational-db-data:
services:
  db:
    image: percona:5.7.31
    restart: always
    volumes:
      - source: relational-db-data
        target: /var/lib/mysql
        type: volume
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: power-interview
  web:
    tty: true
    build: .
    command: bash -lc "bin/rails server"
    links:
      - db
    ports:
      - 3000:3000
    volumes:
      - source: .
        target: /home/app/src
        type: bind
